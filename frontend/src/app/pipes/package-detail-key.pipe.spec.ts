import { PackageDetailKeyPipe } from './package-detail-key.pipe';

describe('PackageDetailKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new PackageDetailKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
