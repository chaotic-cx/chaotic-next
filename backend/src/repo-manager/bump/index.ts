/**
 * Package-bump pipeline: `bump.service` orchestrates rebuild bumps and
 * `bump-config` surgically rewrites the `.CI/config` bump line.
 */
export * from './bump-config';
export * from './bump.service';
